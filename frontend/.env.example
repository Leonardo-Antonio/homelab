VITE_CAMERA_STREAM_URL=http://192.168.31.67/stream

# Backend base URL. Leave empty in production (served same-origin via the Nginx
# proxy). For local dev where the frontend and backend run on different ports,
# point it at the backend so REST and the terminal WebSocket resolve correctly.
# VITE_API_BASE_URL=http://localhost:8080
